<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital@1&family=Roboto:wght@500;700&display=swap" rel="stylesheet">

    <title>My Todo List</title>
</head>
<body>
    <header>
        <h3>To Do List</h3>
    </header>
    <section class="formContainer">
    <h1>To Do List</h1>
    <form action="/addTask" method="POST">
        <input type ='text' placeholder="Add new task" name = 'newTask'>
        <button type="submit">Submit</button>
    </form>
   </section>
   <section class="flexContainer">
    <section>
        <section class="heading">
            <h2>To Do</h2>
         </section>
            <ul class="toDo">
                <% for(let i=0; i < info.length; i++) {%>
                    <li class="item">
                        <% if(info[i].completed === true) {%>
                            <span class='completed'><%= info[i].task %></span>
                        <% }else{ %>
                            <span><%= info[i].task %></span>
                        <% } %>    
                        <span><img src='bin.png' class="bin"></span>
                    </li>
                <% } %>
            </ul>
    </section>
    <section>
         <h2 id="left2Do">Tasks left to do: <%= left %></h2>
    </section>
    
</section>
<script src="main.js"></script>
</body>
</html>